{% assign collection = collections['featured-collection'] %}
{{ collection.title }}

{% for product in collection.products %}
  <li>
  <a href="{{ product.url }}">{{ product.title }} - 
  {{ product.price | money }}</li>
  </a>
{% endfor %}